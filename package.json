{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "shared/*",
      "apps/*"
    ]
  },
  "scripts": {
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "eslint": "^8.1.0",
    "lerna": "^4.0.0",
    "typescript": "^4.4.4"
  }
}
